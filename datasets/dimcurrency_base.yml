unique_name: dimcurrency_base
object_type: dataset
label: dimcurrency_base
columns:
  - name: currencykey
    data_type: long
  - name: currencyalternatekey
    data_type: string
  - name: currencyname
    data_type: string
connection_id: As_Adventure
sql: |-
  SELECT
  "currencykey",
  "currencyalternatekey",
  "currencyname"
  FROM
   SE_DEMO_LIBRARY.AS_ADVENTURE_SMALL."dimcurrency"
  WHERE 
  "currencykey" IN (6, 19, 29, 39, 98, 100)
