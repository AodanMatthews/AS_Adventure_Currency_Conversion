unique_name: Sales Amount YoY
object_type: metric_calc
label: Sales Amount YoY
expression: >-
  (([Date Dimension].[Date Month Hierarchy].CurrentMember, [Measures].[Sales Amount_sum]) - (ParallelPeriod([Date Dimension].[Date Month Hierarchy].[YearMonth], 1, [Date Dimension].[Date Month Hierarchy].CurrentMember), [Measures].[Sales Amount_sum] ))
is_hidden: false
